# API Base URL
# In development: Leave empty to use local proxy (defaults to /api/v1 -> localhost:8000)
# In production: Leave empty to use Nginx proxy (defaults to /api/v1)
# Only set this if hosting API on a different domain/subdomain without a proxy
VITE_API_BASE_URL=
